<%= form_with(model: booking, local: true) do |form| %>
  <% if booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.text_field :name, class:"form-control", placeholder: "Name" %>
  </div>

  <div class="field form-group">
    <%= form.label :date_of_birth %>
    <%= form.date_select :date_of_birth, :start_year => 1970, :end_year => 2025, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.text_field :phone_number, class:"form-control", placeholder: "Phone Number" %>
  </div>

  <div class="field form-group">
    <%= form.label :seat %>
    <%= form.number_field :seat, class:"form-control", placeholder: "Seat" %>
  </div>

  <div class="field form-group">
    <select class="custom-select mr-sm-2" id="inlineFormCustomSelect">
        <option selected>Choose...</option>
        <option value="1">VP3562</option>
        <option value="2">VP3730</option>
        <option value="3">VP2815</option>
      </select>
  </div>

  <div class="field form-group">
    <%= form.number_field :user_id, id: :booking_user_id, class:"form-control", value: current_user.id, type: :hidden %>
  </div>

<br/>
  <div class="actions">
    <%= form.submit class:'btn btn-success' %>
  </div>
<% end %>
